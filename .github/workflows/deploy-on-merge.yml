name: Deploy to Serverless

on:
  push:
    branches:
      - master

env:
  AWS_REGION: eu-north-1

jobs:
  deploy:
    name: Deploy to Serverless
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm install
      - name: Serverless deploy
        uses: serverless/github-action@v3
        with:
          args: deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }} # TODO
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }} # TODO
          AWS_REGION: ${{ env.AWS_REGION }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }} # TODO
          SLACK_SIGNING_SECRET: ${{ secrets.SLACK_SIGNING_SECRET }} # TODO
